const DOMAIN = "http://localhost:8080";

const AUTH = "auth";
export const AUTH_LOGIN_URL = `${DOMAIN}/${AUTH}/login`;

const CUSTOMERS = "customers";
export const CUSTOMERS_BASE_URL = `${DOMAIN}/${CUSTOMERS}`;

const USERS = "users";
export const USERS_BASE_URL = `${DOMAIN}/${USERS}`;

const ITEM_CATEGORIES = "item-categories";
export const ITEM_CATEGORIES_BASE_URL = `${DOMAIN}/${ITEM_CATEGORIES}`;

const ITEM = "items";
export const ITEMS_BASE_URL = `${DOMAIN}/${ITEM}`;

const STOCK = "stocks";
export const STOCKS_BASE_URL = `${DOMAIN}/${STOCK}`;

const ORDER = "orders";
export const ORDERS_BASE_URL = `${DOMAIN}/${ORDER}`;
